# Ashita settings (per-character, not shared)
config/
config.dat

# Python cache
__pycache__/
*.pyc

# Local export config (copy from config.default.py)
scripts/config.py

# Extracted textures (dev artifacts, addon loads from DATs)
textures/
scripts/dump-texture.lua
